class Moves:
	def __init__(self, move_id, name, move_type, power, accuracy, pp, effect):
		self.move_id = move_id
		self.name = name
		self.move_type = move_type
		self.power = power
		self.accuracy = accuracy
		self.pp = pp
		self.effect = effect


	def power(self):
		pass